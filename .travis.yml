language: php

php:
  - 7.2
  - 7.3
  - 7.4snapshot
  - nightly

matrix:
  allow_failures:
    - php: nightly

sudo: false

install:
  - travis_retry composer install --no-interaction --prefer-source

script:
  - composer test
